# Module 1：PBridge 默认方案验证

## 默认逻辑

正式 PBridge 采用 B 方案。PPR-MoE 输出的预测分数同时用于：

1. 对 20 条候选知识进行 Top-3 排序；
2. 对 Top-3 知识做 `softmax(score / 0.5)` 加权；
3. 构造 3 个 prefix token。

Gold Utility 只用于训练排序损失，不进入测试时的 prefix 构造。Lingshu-7B
和 GME 冻结，PPR-MoE 训练损失已收敛。

## 正式测试集

PediatricsMQA 的固定 8:2 划分为 1,654/413。Lingshu-7B 使用同一批
No-RAG 结果作为基线。

| 方法 | 正确数 | Acc | 相对 No-RAG |
|---|---:|---:|---:|
| No-RAG | 253/413 | 61.26% | — |
| **PBridge（B）** | **285/413** | **69.01%** | **+7.75 pp** |

PBridge 相对 No-RAG 修正 74 题、损伤 42 题，净增加 32 题；配对精确检验
`p=0.0038`。NLL 为 2.8160，Brier 为 0.5836；本实验只把准确率作为主指标。

## 多随机种子

三组都使用完整 1,654 道训练题，Stage 1 和 Stage 2 均达到原损失收敛条件。

| 模型种子 | Stage 1 最低轮次 | Stage 2 最低轮次 | Test Acc | 净增益 |
|---:|---:|---:|---:|---:|
| 20260918（默认） | 2115 | 28 | 69.01% | +7.75 pp |
| 20260919 | 2164 | 21 | 66.83% | +5.57 pp |
| 20260920 | 2330 | 14 | 68.04% | +6.78 pp |

三次净增益均为正，范围为 `+5.57～+7.75 pp`，平均 `+6.00 pp`。

## 不同数据划分

另外按 `img_id` 分组做两次 80/20 划分，训练图像和留出图像完全不重叠。
每次均在未见过的图像留出集上测试，训练损失也已收敛。

| 划分种子 | 训练/留出题数 | 图像交集 | No-RAG | PBridge | 净增益 |
|---:|---:|---:|---:|---:|---:|
| 20260920 | 1316/338 | 0 | 57.40% | 69.82% | **+12.43 pp** |
| 20260921 | 1315/339 | 0 | 58.70% | 72.27% | **+13.57 pp** |

两次图像级独立留出测试的净增益均为正，范围为 `+12.43～+13.57 pp`，
平均 `+13.00 pp`，配对精确检验分别为 `p<0.00001`。

## 结论

B 应作为唯一默认方案保留。它在 3 个随机种子和 2 个图像级独立划分上
都相对同口径 No-RAG 获得正净增益；完整训练集的增益约为 6–8 pp，
图像级独立留出集的增益约为 12–14 pp。旧的 Utility 加权、A/C 版本及其
中间 checkpoint 不再属于当前算法。
